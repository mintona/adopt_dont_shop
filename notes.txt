<% if @pets == [] %>
  <h1> <%= @shelter.name %> Pets </h1>
  <%= link_to 'Add Pet', "/shelters/#{@shelter.id}/pets/new" %>

<% elsif @pets.length == Pet.all.length && Pet.all.length != 1 %>
  <h1>All Pets</h1>
    <% @pets.each do |pet| %>
      <h2>Name: <%= pet.name %></h2>
      <%= image_tag(pet.image, :alt => "#{pet.name} image") %>
      <p>Approximate Age: <%= pet.approximate_age %> </p>
      <p>Sex: <%= pet.sex %> </p>
      <p>Current Shelter: <%= pet.shelter.name %> </p>
    <% end %>


<% elsif @shelter.pets.length == 1 && Pet.all.length == 1 %>
  <h1> <%= @shelter.name %> Pets </h1>
  <% @shelter_id = @pets.first.shelter_id %>
  <% @pets.each do |pet| %>
    <h2>Name: <%= pet.name %></h2>
    <%= image_tag(pet.image, :alt => "#{pet.name} image") %>
    <p>Approximate Age: <%= pet.approximate_age %> </p>
    <p>Sex: <%= pet.sex %> </p>
  <% end %>
  <%= link_to 'Add Pet', "/shelters/#{@shelter_id}/pets/new" %>


<% elsif @pets == @shelter.pets %>
  <h1> <%= @shelter.name %> Pets </h1>
    <% @shelter_id = @pets.first.shelter_id %>
    <% @pets.each do |pet| %>
      <h2>Name: <%= pet.name %></h2>
      <%= image_tag(pet.image, :alt => "#{pet.name} image") %>
      <p>Approximate Age: <%= pet.approximate_age %> </p>
      <p>Sex: <%= pet.sex %> </p>
    <% end %>
  <%= link_to 'Add Pet', "/shelters/#{@shelter_id}/pets/new" %>


<% end %>




ArgumentError:
       Invalid option(s) :alt, should be one of :above, :below
, :left_of, :right_of, :near, :count, :minimum, :maximum, :bet
ween, :text, :id, :class, :style, :visible, :obscured, :exact,
 :exact_text, :normalize_ws, :match, :wait, :filter_set
